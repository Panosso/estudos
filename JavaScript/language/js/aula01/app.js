// alert("Olá, mundo!");

// console.log("Olá, mundo!");

// Variáveis que podem ter seus valores alterados
let age = 30;
let currentYear = 2026;
var name = "Maxado"; // Variável usando var, não é recomendada

const pi = 3.14; // Variável constante, não pode ser alterada

console.log(age, currentYear);
console.log(pi); // Isso causará um erro, pois pi é uma constante
// pi = 3 // Isso causará um erro, pois pi é uma constante

